export enum AppRoutes {
  AUTH = "/auth",
  ACCOUNT = "/account",
  USER = "/user",
  WORKSPACE = "/workspace",
  PATIENTS = "/patients",
}

export enum RoutesIBGE {
  ESTADOS = "/estados",
  CIDADES = "/municipios"
}